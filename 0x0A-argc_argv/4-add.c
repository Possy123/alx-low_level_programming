#include <stdio.h>
#include <stdlib.h>

/**
 * main - main function
 * @argc: counts argument
 * @argv: prints variables
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i = 1;
	int j = 0;

	if (argc == 1)
	{
		printf("0\n");
		return (0);
	}
	else
	{
		while (i < argc)
			char *argv = argv[i];
		while (
