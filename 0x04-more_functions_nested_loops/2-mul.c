#include <stdio.h>
#include "main.h"

/**
 * mul - multiplication of two digits
 * @a: first digit
 * @b: second digit
 *
 * Return:result
 */
int mul(int a, int b)
{
	int result = a * b;
		return (result);
}
