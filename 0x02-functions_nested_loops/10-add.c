#include <stdio.h>
#include "main.h"

/**
 * add - add numbers
 *
 * @a: furst number
 * @b: second number
 *
 * Return: result of a + b
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
